// KB2.1 Core Service Middleware Build File

archivesBaseName  = 'kb2-core'
description       = 'Knowledge.Bio Release 2.1 Core Service Middleware'

evaluationDependsOn(':services')

dependencies {
	compile project(':services')
}
