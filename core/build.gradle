archivesBaseName  = 'tkbio-core'
description       = 'NCATS Translator Knowledge.Bio (TKBio) Core Spring Service Layer Middleware'

evaluationDependsOn(':services')

dependencies {
	compile project(':services')
}
