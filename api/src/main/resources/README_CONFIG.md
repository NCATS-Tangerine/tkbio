Copy the properties file templates from the 'config' folder of the project, 
into the src/main/resources folder, removing the '.template' file extension, 
then customize them to local site conditions.